# Versions are the ones the project was developed and evaluated with, on Python 3.9.
# TensorFlow is only needed for the neural network stage. The classical pipeline
# runs without it, though Controller.py imports it unconditionally.

PySide6==6.7.0
numpy==1.26.4
scipy==1.13.0
scikit-image==0.22.0
scikit-learn==1.4.2
opencv-python==4.9.0.80
pillow==10.3.0
matplotlib==3.8.4
cffi==1.16.0
screeninfo==0.8.1
tensorflow==2.10.0
